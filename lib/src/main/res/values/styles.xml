<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="tanghy_view_cascade_dialogTheme" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 遮罩层 -->
        <item name="android:backgroundDimAmount">0</item>
    </style>
    <!--弹出框动画-->
    <style name="tanghy_view_cascade_dialog_animStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/tanghy_view_cascade_dialog_in_anim</item>
        <item name="android:windowExitAnimation">@anim/tanghy_view_cascade_dialog_out_anim</item>
    </style>
</resources>